const oddsAndEvens = (array) => array.sort((a, b) => a - b);

const sorteredNumbers = oddsAndEvens([13, 3, 4, 10, 7, 2]);
console.log(`os números ${sorteredNumbers} estão ordenados de forma crescente`);
